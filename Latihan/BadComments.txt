1. Journal Comments (Bad Comments)

    /*
    * Changes (from 11-Oct-2001)
    * --------------------------
    * 11-Oct-2001: Reorganised the class and moved it to new
    * package com.jeffrey.y.date (DG);
    * 05-Nov-2001: Added getDescription() method, and
    * eliminated NotADate class (DG);
    * 12-Nov-2001: IBD requires setDescription() method, now
    * that NotADate class is gone (DG); Changed
    * getPreviousDayOfWeek().
    * getFollowingDayOfWeek(), and
    * getNumberOfDaysInMonth() to correct bugs (DG);
    * 05-Dec-2001: Fixed bug in SpreadsheetDate class (DG);
    * 29-May-2002: Moved the month constants into a separate
    * interface (MonthConstants) (DG);
    */

2. Noise Comments (Bad Comments)
    /**
    * Default constructor.
    */
    /*
    protected AnnualDateRule() { }
    */

    /**
    * The day of the month.
    *
    * @return the day of the month.
    */
    private int dayOfMonth;

    public int getDayOfMonth() {
        return dayOfMonth;
    }
